# 安全声明

> 本文件定义 KCode 的**采集边界、隐私开关语义与验证方式**。
> 每一项承诺都必须能被第三方验证；无法验证的承诺不写入本文件。

---

## 一、核心承诺

**KCode 不会在你的机器上采集工作区数据并发送到任何地方。**

这不是配置项的默认值，而是架构级约束：

> **Agent 运行时之外，不得存在任何读取工作区文件并向网络发送数据的宿主进程或后台任务。**

所有文件读取都发生在 app-server 子进程内，并作为 Item 事件对用户可见。

---

## 二、出站连接清单

### 你显式配置的目标

| 目标 | 触发条件 | 用途 |
|---|---|---|
| 模型 provider（你配置的 base URL） | 你提交任务时 | 发送对话上下文以获取模型响应 |
| MCP server（你配置的） | 你启用该 server 时 | 工具调用 |
| Git remote（你配置的） | 你执行 push/fetch 时 | 版本控制同步 |

### ⚠ 已关闭的一处默认过宽写入面（实测发现）

**`codex` 的 `workspace-write` 沙箱默认把临时目录也纳入可写集合。**

实测确认（真实模型驱动）：

| 目标 | 默认配置 | 说明 |
|---|---|---|
| 工作区 | 可写 | 预期行为 |
| `$TMPDIR` 及同级目录 | **可写，且不弹审批** | 非预期——UI 未提示 |
| 其他工作区外路径 | 阻止或走审批 | 预期行为 |

相关字段是 `SandboxPolicy` 的 `excludeTmpdirEnvVar` 与 `excludeSlashTmp`，
**默认值都是 `false`**。这意味着用户以为「工作区可写」等价于「只动项目」，
实际却存在一个**静默的写入面**：临时目录可被 Agent 任意写入而不触发审批，
也不产生任何可见提示。

**KCode 的处置：**

1. **默认关闭**：应用写入隔离 `config.toml` 时显式设置
   ```toml
   [sandbox_workspace_write]
   exclude_tmpdir_env_var = true
   exclude_slash_tmp = true
   ```
   实测确认关闭后区外写入回到审批/阻止路径，且**工作区内写入不受影响**。

2. **可验证**：`bash scripts/verify-sandbox-boundary.sh` 用真实模型驱动三个探针
   （`$TMPDIR` 写入、区外同级目录写入、工作区内写入），前两者必须被阻止、
   第三者必须正常。脚本已接入 CI。

3. **不隐瞒**：本节如实记录默认行为的差异，以及我们改变了什么。

### ⚠ 上游组件的一处未配置出站连接（实测披露）

**我们实测发现：`codex` CLI 在建立会话时会尝试连接 OpenAI 的 CDN 段
（`2a03:2880::/32`，`api.openai.com` / `chatgpt.com` 所在段，443 端口）。**

这一点必须如实说明，因为它**不属于上表「你显式配置的目标」**：

| 项 | 实测结果 |
|---|---|
| 触发时机 | `thread/start` 期间（启动 app-server 本身不触发） |
| 触发条件 | **无条件**。与模型名、provider 配置、feature flag 均无关（已逐一对照实验） |
| 连接结果 | 停留在 `SYN_SENT`，在本机网络环境下未建立 |
| 数据内容 | 未观测到数据传输（连接未建立）；推测为模型元数据查询 |
| 来源 | **codex CLI 自身行为，非 KCode 引入**（KCode 只是托管它的进程） |
| 可否关闭 | 未找到关闭开关。`api_key_model_discovery` 默认已关闭且非该连接的来源 |

**KCode 的处置：**

1. **不隐瞒**：本文件与 `scripts/verify-no-egress.sh` 都显式记录该行为，脚本把它列入白名单正是为了让这一事实可见而非被静默忽略。
2. **可验证**：运行 `bash scripts/verify-no-egress.sh` 会实时抓取连接表并分类报告（本地 provider / OpenAI CDN / 其他）。任何**计划外**的连接都会导致脚本以非零码退出。
3. **可规避**：若你的环境要求严格零外发，可在防火墙或 hosts 层阻断 `2a03:2880::/32`。实测该连接失败不影响本地 mock provider 下的完整功能验证（连接本就未建立）。

**除上表与上述 CDN 段之外，没有任何其他出站连接。** 没有遥测、没有崩溃上报、没有用量统计、没有「体验优化」后台任务、没有工作区快照。

### 如何自行验证

本仓库提供可执行的验证脚本，无需手工取证：

```bash
bash scripts/verify-no-egress.sh
```

它会驱动一轮完整的真实交互（含审批），轮询抓取 app-server 进程的全部网络连接，
并按三类结果报告：本地 provider / OpenAI CDN 段 / **计划外连接**。
只有第三类非空时脚本才失败——那才是需要追查的静默外发。

也可用任意抓包工具（Wireshark / Little Snitch / `tcpdump`）自行监控该进程树。

---

## 三、隐私开关语义

本产品的原则是：**每个开关都必须有单一、可验证的语义，且关闭后行为必须真正停止。**

### 遥测开关

| 状态 | 具体不发生什么 |
|---|---|
| **关闭（默认）** | 不发送任何崩溃报告、用量数据、性能指标。不建立相关连接。 |
| 开启（需显式选择） | 仅发送崩溃堆栈与版本号。**绝不包含**代码内容、文件路径、仓库名、命令内容、模型输入输出。 |

**验证方式**：关闭状态下抓包，确认无到遥测端点的连接。

### 关于 app-server 自身的遥测

codex app-server 的遥测**默认关闭**，需显式 opt-in（官方 `--help` 原文：*Analytics are disabled by default for app-server. Users have to explicitly opt in*）。KCode **不会**为你开启它。

### 若未来引入本地快照 / checkpoint

约束（对应落地方案 6.1 第 1 条）：

- 只允许**本地增量**快照；
- 必须能一键关闭，且关闭后**采集动作真正停止**（不是只隐藏 UI）；
- 关闭路径必须有**代码级测试**覆盖；
- 快照内容可被用户自行查看与删除。

---

## 四、我们明确不做的事

| 不做 | 说明 |
|---|---|
| 打包工作区上传 | 任何形式都不做 |
| 脱离工具循环的宿主级采集 sidecar | 不做；这是本项目与「有采集行为的工具」的根本区别 |
| 默认开启的代码库索引 | 不做 |
| 上传 `.git` 历史 / LFS / reflog | 不做 |
| 服务端持有密钥的加密上传 | 不做；若做加密存储，私钥必须在用户手里 |
| 认证/身份绑定之外的账户关联 | 不做 |
| 全局永久「永不询问」 | 不提供该选项 |

---

## 五、凭据处理

- 凭据只存 **OS keychain**（macOS Keychain / Windows Credential Manager / Linux Secret Service）。
- 不写入明文配置文件，不进入日志，不进入事件日志。
- 写入任何持久化存储前先脱敏。
- 你随时可以在系统钥匙串中直接删除。

## 六、审批与脱敏

- 审批弹窗默认对 `.env`、私钥、token、`Authorization` 头、连接串做掩码；「点击显示」操作会记入审计日志。
- 「始终允许」必须绑定作用域（当前 Thread / Project / Profile / 全局），不提供全局永久放行。
- 自动批准同样写入审计日志。

## 七、审计

- 所有命令、补丁、审批决策、模型与退出码均写入本地审计日志。
- 审计日志**永不离开本机**，除非你主动导出。
- 可一键导出为 JSON、可清空、可设置保留周期。

## 八、沙箱

| 平台 | 机制 | 备注 |
|---|---|---|
| macOS | Seatbelt（`sandbox-exec`） | 系统级强制 |
| Linux | `bwrap` 只读根 + `PR_SET_NO_NEW_PRIVS` + seccomp 网络过滤 | 系统级强制 |
| Windows | 受限令牌 + AppContainer | **官方标记高度实验性** |

**Windows 用户请注意**：官方沙箱在 Windows 上不可靠（Everyone SID 已有写权限的目录无法被可靠限制）。若需 Windows 生产使用，请在 KCode 之外叠加 Hyper-V / WSL / 容器或独立低权限账户。

## 九、供应链

- 协议 schema 与 TypeScript 绑定由 CI 冻结，协议变更在 PR diff 中可见。
- codex CLI 版本与 SHA256 由 `codex.lock.json` 锁定，启动时校验。
- 安全下限版本 `0.39.0`（CVE-2025-59532 修复版本）；低于此版本会因沙箱路径配置缺陷，把模型生成的 `cwd` 当作可写根。
- CI 每周检查上游版本更新与安全公告。

## 十、如何自行验证本文件的每一条

本文件的所有承诺都对应可执行的验证手段：

```bash
npm test                          # 版本基线 + 端到端契约（离线、零凭据）
npm run check:drift               # 协议未被静默篡改
lsof -i -a -p <kcode-pid>         # 出站连接仅为你配置的目标
```

此外，客户端完整开源，可复现构建，第三方可审计「代码里没有采集逻辑」。

---

## 报告安全问题

如发现本文件中任何承诺与实际行为不符，**这是最高优先级的问题**，请通过仓库 Issues 报告，并在标题标注 `[SECURITY]`。

若能提供抓包或文件系统监控证据，将极大加快确认。架构级承诺的违背，比任何功能性缺陷都严重——本产品的全部价值都建立在「你能验证我在做什么」之上。

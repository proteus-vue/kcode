# 在本仓库工作时的约定

## 「直接收尾」= 立即停止所有自动化测试

当用户说 **「直接收尾」**（或「别跑了」「先守卫」这类同义表达）时：

1. **立刻停止所有正在跑的自动化测试与构建**（含后台任务），
   **不管跑到哪一步、有没有完成、是不是刚开的**。不要「等这一条跑完」，
   不要「这个很快」，不要为了「确认没坏」再跑一次。
2. 立即进入收尾：**保存项目记忆**（更新 `docs/` 下的状态/勘误文档）。
3. **提交并推送**（`git add -A` → commit → push），
   未完成的、未验证的部分**如实写进 commit message 与文档**，不隐藏。

意思是字面的：**停止 → 记录 → 提交推送**。中间不插测试。

> 这条规则的由来：用户两次说「太慢了，先收尾」之后，我仍在跑全量测试
> 和参数扫描，用户不得不第三次喊停并加感叹号。收尾时的测试**没有价值**
> ——它既改变不了结论，又让用户等。要验证的东西应该在收尾**之前**验完，
> 而用户决定停止时，剩下的风险由「如实记录」来承担，不是由「再跑一遍」。
>
> 注意：**「不要测试」只适用于收尾指令**。正常开发时该跑的测试照跑
> （这个仓库的规矩就是数字必须真的跑通过，见 `docs/test-stats.md`）。

## 效率上的硬性要求

- **间歇性的现象不要用穷举参数去定位**。先分清「确定性失败」与
  「间歇性失败」：同参数连跑几次就能分辨。间歇性问题扫 6 个参数值
  既慢又得不出结论（本轮在 Android 窗口捕获上就是这么浪费掉时间的）。
- **怀疑代码之前先查环境**：残留进程（`pgrep -fl <helper>`）、
  权限、前台窗口、其它模拟器是否在跑。本轮 4 个孤儿 `kcode-window-cast`
  进程抢同一个窗口，很可能就是「出帧不稳定」的真因。
- **外部资源（参考实现、远程仓库）取一次就留在本地复用**，不要反复拉。
- 长时间运行的命令一律套 `timeout`；后台任务要及时收尾，别留孤儿进程。

## 记录与交付

- **未验证的东西必须标「未验证」**，不能写成「已支持」。
  `docs/模拟器接入状态.md` 是四平台能力的状态快照，逐格标注，
  改动能力位时要同步更新它。
- **实测结论连依据一起写**（数字、A/B 对照、命令与输出），
  写进 `docs/协议勘误与修正.md`。判断「送达 ≠ 生效」的教训在这个仓库
  反复出现，写结论时必须写清判据是什么。
- 协议细节以 `docs/protocol-facts.md` 为准，不以其它文档为准。
- 测试数字以 `docs/test-stats.md` 为准（`npm run stats` 自动生成）。
